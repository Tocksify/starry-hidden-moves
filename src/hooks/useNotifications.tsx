// Notifications provider — placeholder after multiplayer system rewrite.
// Friend/challenge notifications removed; the new multiplayer uses room codes.
export function NotificationsProvider() {
  return null;
}
